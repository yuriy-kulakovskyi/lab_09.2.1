#include <stdio.h>
#include "../include/main.h"

int main() {
  int x, y, z;
  float w;
  char ch;

  puts("Program to calculate expression based on conditions");
  check(x, y, z, w, ch);

  return 0;
}